// eslint.config.js — configuração para Node.js/Express (ESLint 9+ flat config)
const js = require('@eslint/js');

module.exports = [
  js.configs.recommended,
  {
    languageOptions: {
      ecmaVersion: 2022,
      sourceType:  'commonjs',
      globals: {
        // Node.js globals
        require:    'readonly',
        module:     'readonly',
        exports:    'readonly',
        __dirname:  'readonly',
        __filename: 'readonly',
        process:    'readonly',
        console:    'readonly',
        setTimeout: 'readonly',
        clearTimeout: 'readonly',
        setInterval:  'readonly',
        clearInterval:'readonly',
        Buffer:     'readonly',
        // Jest globals
        describe:   'readonly',
        test:       'readonly',
        it:         'readonly',
        expect:     'readonly',
        beforeEach: 'readonly',
        afterEach:  'readonly',
        beforeAll:  'readonly',
        afterAll:   'readonly',
        jest:       'readonly',
      },
    },
    rules: {
      // Erros reais
      'no-unused-vars':    ['warn', { argsIgnorePattern: '^_', varsIgnorePattern: '^_' }],
      'no-console':        ['warn', { allow: ['warn', 'error'] }],
      'no-undef':          'error',
      'no-duplicate-case': 'error',
      'no-unreachable':    'error',

      // Boas práticas
      'eqeqeq':            ['error', 'always'],
      'no-var':            'error',
      'prefer-const':      'warn',
      'no-throw-literal':  'warn',   // projeto usa throw { status, mensagem } nos services

      // Estilo (delegado ao Prettier — desligados aqui)
      'indent':            'off',
      'quotes':            'off',
      'semi':              'off',
    },
  },
  {
    // Ignora pastas que não devem ser lintadas
    ignores: [
      'node_modules/**',
      'coverage/**',
      'dist/**',
    ],
  },
];